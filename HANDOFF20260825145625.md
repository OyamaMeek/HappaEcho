# HappaEcho 开发交接

> 更新时间：2026-08-25 14:56:25
> 面向对象：完全没有本会话上下文、需要直接继续开发的新 Claude Code 会话

## 1. 我们在做什么

当前项目是 **HappaEcho**：一款原生 iOS/iPadOS AI 聊天应用。正式规格和实施计划位于：

- [设计规格](docs/superpowers/specs/2026-08-23-happaecho-ios-design.md)
- [实施计划](docs/superpowers/plans/2026-08-23-happaecho-ios-implementation.md)

核心约束：

- 产品名、Xcode target、scheme 都必须是 `HappaEcho`，不要使用旧名 `OpenCat`。
- 最低部署版本是 iOS 17.0。
- 使用 SwiftUI、SwiftData、URLSession、Security/Keychain、PhotosUI、UniformTypeIdentifiers。
- SwiftData 是本地唯一事实来源；Notion 只是单向备份。
- Chat API key 和 Notion token 只能放在 Keychain；`AppSettings` 不存秘密。
- 图片原始字节保存于 Application Support，不压缩、不转码、不把二进制写入 SwiftData。
- 用户消息必须先持久化，再开始流式请求；消息持久化后立即具备 Notion 同步资格。
- 含图片消息在模型没有视觉能力时，必须在请求前阻止，不能静默降级为纯文字。
- Notion 同步按对话串行、不同对话可并发；必须有持久化检查点、确定性 marker、分页对账和有限重试。
- 用户已经选择：Notion 重试为三次总尝试；数据库切换时完整重放历史；附件上传完成后追加 Notion image block。
- 用户偏好：真正的产品/架构取舍使用弹窗选项；既定计划内的常规实现不要反复询问。
- 用户明确要求：后续开发直接在项目主目录，不使用 sandbox/worktree 开发。

## 2. 当前仓库状态

主目录：

```text
/Users/oyamahappa/Documents/GitHub/HappaEcho
```

当前分支：`main`

最近提交：

```text
bc1382d merge: integrate HappaEcho implementation
89c5463 docs: add development handoff
 e79f1b5 fix: recover interrupted Notion uploads
```

主目录已包含截至 Task 9 的实现代码和测试。当前 `git status` 只显示 `?? .claude/`；这是旧会话留下的本地目录，不要未经检查删除，也不要把其中的 worktree 当作后续开发位置。用户要求直接修改主目录。

本交接文件本身是新文件，写完后应单独提交；提交前检查是否有其他未预期改动。未经用户明确要求，不 push、不创建 PR、不删除分支。

工程入口：

```text
HappaEcho.xcodeproj
```

## 3. 已经完成的任务

### Task 1：Xcode 工程和应用壳

已完成并合入主分支：

- `HappaEcho.xcodeproj`
- app、unit test、UI test targets
- `HappaEchoApp`、`ContentView`
- iOS 17 deployment target
- 相机和照片权限文案
- shared scheme 和基础构建配置

### Task 2：SwiftData 数据模型

已完成：

- `Conversation`
- `Message`
- `MessageAttachment`
- `NotionPageBinding`
- `AppSettings`
- `HappaEchoSchema`
- 领域枚举、同步状态、生成状态
- 内存容器和持久化/级联删除测试

模型已经包含 Notion 批次游标、确认 block ID、附件上传状态、页面绑定历史、标题生成状态等字段。

### Task 3：Keychain 和设置仓库

已完成：

- `CredentialStore`
- `KeychainService`
- `SettingsRepository`
- Chat API key 与 Notion token 的增删改查
- 使用 generic password、固定 service 和设备解锁后可用的 accessibility
- 测试使用隔离账户名，不碰真实账户

### Task 4：SSE 和 OpenAI-compatible 流式客户端

已完成：

- SSE 增量解析，支持分片 UTF-8、CRLF/LF、多 data 行、`[DONE]`
- OpenAI-compatible Chat Completions 请求和错误映射
- URLSession streaming transport
- 取消时从 consumer 传递到 transport
- 取消、正常结束、错误结束只完成一次
- parser 和 client 测试

### Task 5：原图附件和多模态请求体

已完成：

- 文件、Photos、相机导入
- 保留来源原始字节
- Application Support 文件存储、相对路径、清理和孤儿处理
- `.current` Photos representation
- iCloud file representation 的真实 Progress、取消和 KVO 生命周期处理
- 多模态 JSON 请求体使用临时文件和准确 Content-Length
- 每次重试重新打开 body stream
- MIME、大小上限、路径 containment、安全作用域和清理测试

### Task 6：聊天会话控制器

已完成：

- 用户消息先保存
- 发送、流式 assistant 内容累计、完成/停止/部分失败状态
- 生成取消和会话状态机
- 图片上下文和序列校验
- Notion 调度接口接入点

### Task 7：自动标题和手动标题保护

已完成：

- 首个完整 assistant 回复后自动生成标题
- 手动标题不会被模型覆盖
- 标题生成失败保留合理回退
- 失败和抛错服务测试

### Task 8：Notion formatter 和 HTTP client

已完成：

- Markdown/消息格式化为 Notion blocks
- 角色、时间、代码语言等元信息
- 多批 block 和 batch 上限校验
- page properties
- opaque cursor 正确编码
- Notion headers 和 HTTP 错误映射
- multipart file upload，调用方 MIME type 正确传递
- Retry-After 秒数和三种 HTTP-date 格式
- Notion image/file_upload block 序列化

### Task 9：幂等 Notion 同步协调器

已完成并提交：`e79f1b5` 以及其前置 Task 9 提交。

已实现：

- actor 管理每个 conversation 的 FIFO worker
- 同一对话串行，不同对话可并发
- worker token 防止旧 worker 清理新 worker 状态
- cancel 不会在旧 worker 的 in-flight 网络操作退出前启动第二个 worker
- disabled settings 保持 pending，不做网络工作
- `resumePending()` 扫描待同步消息
- page 创建、绑定和 metadata 更新
- 数据库切换时新建页面、重置页面检查点、完整重放历史
- 文本 batch marker 对账和分页查询，避免响应丢失导致重复 append
- 远端 block ID 回写本地 checkpoint
- 附件 create/send/complete/image block 分阶段持久化
- image caption marker 对账，避免重复 image block
- `retrieveFileUpload(uploadID:)` 恢复接口
- 已保存 send intent 但远端仍 pending 时重新发送
- 远端已 uploaded 时跳过重复发送和 complete，直接继续 image block
- transient error 三次总尝试，保留最终错误

最近聚焦验证：Notion 相关测试 **28 tests, 0 failures**。注意：某次完整运行遇到 CoreSimulator `Invalid device state (code 405)`，属于模拟器环境问题，不是代码断言失败；重新运行前先确认模拟器状态。

## 4. 当前卡在哪

从产品实现角度，Task 1–9 已完成；实施计划后续还有 Task 10–14。当前没有正在进行的代码修复，但以下事项是下一阶段的实际入口：

1. **Task 10：设置界面和连接测试**尚未实现。
   - Notion 开关、数据库 ID、Chat endpoint、模型、API key/token 输入。
   - 秘密仍只能通过 Keychain repository 读写。
   - 连接测试要有清晰的 loading/success/error 状态。
   - 必须确保 SwiftData 只有一个 `AppSettings` 记录。

2. **Task 11：富文本渲染和设计系统**尚未实现。
   - Markdown、代码块、表格、LaTeX、原文 fallback。
   - 深色 productivity-glass 视觉系统。
   - 不要为了渲染引入不必要的第三方依赖，遵守计划中的单 target 约束。

3. **Task 12：iPhone/iPad 自适应工作区**尚未实现。
   - iPhone 使用 stack/navigation；iPad 使用 sidebar/detail 或 split layout。
   - 长回答、代码、公式必须优先可读。
   - 需要实际启动 app，在浏览器/模拟器中验证 UI golden path 和边界。

4. **Task 13：依赖组装和生命周期恢复**尚未实现。
   - composition root
   - app lifecycle 恢复待同步队列
   - conversation/message/attachment 删除协调
   - UI 与 Notion/chat/attachment 各层接线
   - 手动验收清单

5. **Task 14：最终发布验证**尚未实现。
   - 全规格逐项核对
   - 完整 unit/UI test
   - Release build
   - 最终全分支 code review
   - 真机和云端照片项目的手动验收

## 5. 下一步计划

严格按以下顺序执行，直接在主目录：

### 第一步：基线检查

```bash
git status --short
xcodebuild -list -project HappaEcho.xcodeproj
```

确认没有未预期的主目录改动后，阅读：

- `docs/superpowers/specs/2026-08-23-happaecho-ios-design.md`
- `docs/superpowers/plans/2026-08-23-happaecho-ios-implementation.md`
- 本文件

### 第二步：完成 Task 10

遵守 TDD：先为设置状态、Keychain 绑定、单例 AppSettings、连接测试写失败测试，再实现最小设置 UI。实现后运行聚焦测试，并启动 app 做实际 UI 验证。

### 第三步：完成 Task 11–13

每个任务都要：

1. 先写失败测试。
2. 运行确认测试确实因缺失行为失败。
3. 写最小实现。
4. 运行聚焦测试和相关回归测试。
5. 对 UI 变化启动 app，实际操作 golden path 和边界。
6. 每个任务单独提交。
7. 做独立规格/质量审查，重要发现进入修复循环。

### 第四步：完成 Task 14

运行：

- 完整 unit tests
- UI smoke tests
- Debug/Release build
- Notion、附件、流式取消、Keychain 回归测试
- 最终代码审查

模拟器若报 `Invalid device state`，先处理或重启 CoreSimulator；不要据此修改生产代码。

## 6. 已踩过的坑，绝对不要再踩

### 6.1 不要再使用 sandbox/worktree 开发

用户已经明确要求直接放在项目主目录。不要调用 `EnterWorktree`，不要在 `.claude/worktrees` 中继续实现。所有新代码、测试和提交都在：

```text
/Users/oyamahappa/Documents/GitHub/HappaEcho
```

### 6.2 不要删除或覆盖未知的主目录改动

每次编辑前先看 `git status --short`。不要用 `git reset --hard`、`git clean -fdx`、`restore .` 或类似破坏性命令。尤其不要删除 `.claude/`，除非用户明确要求并确认其内容已不需要。

### 6.3 不要把主目录和旧 worktree 混用

旧 `.claude/worktrees/happaecho-ios-implementation` 可能仍存在，但它只是历史开发区。主目录 `main` 已合入实现；后续不要从旧 worktree 分支继续提交，也不要把它当成 source of truth。

### 6.4 不要重复创建 Xcode 工程或改 target 结构

现有 `HappaEcho.xcodeproj` 已包含当前源文件和测试 target membership。新增文件必须加入正确 target；先读 `project.pbxproj` 的现有组织方式。

### 6.5 不要跳过 TDD 和独立审查

此项目此前多次因审查发现真实问题而修复：文本重复 append、数据库切换未完整 replay、附件测试没有真实源文件、取消时旧 worker 和新 worker 并发、send intent 导致失败上传永久卡死。新任务必须先写能失败的测试，再实现；不能只凭手动验证或“看起来正确”。

### 6.6 Notion append 不能盲目重试

任何 append 响应丢失都可能造成重复远端 block。先用稳定 marker 分页查询；找到 marker 后只补本地 checkpoint；没有 marker 才 append。总尝试次数最多三次。

### 6.7 附件 send 状态不能只看本地时间戳

`notionUploadSentAt` 代表本地 send intent，不等于 Notion 已收到文件。恢复时必须查询 `retrieveFileUpload`：

- 远端 `uploaded`：跳过重复 send 和 complete。
- 远端仍 pending/unready：允许重试 send。
- 查询和发送错误要保留可恢复状态，不能永久把任务标成已完成。

### 6.8 不要用测试桩掩盖生产取消 bug

取消测试必须验证真实 transport cancellation、stream completion 和 continuation 生命周期。不能只通过让 URLProtocol `stopLoading()` 伪造完成，也不能删掉挂起测试或弱化断言。

### 6.9 不要把异步 actor 调用直接放入 XCTest autoclosure

错误示例：

```swift
XCTAssertEqual(await service.count(), 2)
```

应先 await 到局部变量，再断言：

```swift
let count = await service.count()
XCTAssertEqual(count, 2)
```

### 6.10 不要让附件测试只创建元数据

生产 `sendFile` 会从 `fileURL` 读取真实文件。测试 fixture 必须在与生产相同的 Application Support 路径写入真实字节，并让 fake 读取该路径；清理只能删除本测试 conversation 的目录。

### 6.11 不要忽略 SwiftData actor 隔离

`ModelContext`、SwiftData models 和附件路径解析只能留在 `@MainActor` store 边界；actor 中使用 Sendable snapshot，不要把 ModelContext 跨 actor 传递。

### 6.12 不要把秘密写入 SwiftData、日志或文档

API key、Notion token 不能进入 `AppSettings`、测试输出、提交内容或交接文档。测试使用随机/隔离账户和 synthetic endpoints。

### 6.13 不要把模拟器环境故障当作产品失败

CoreSimulator `Invalid device state (405)` 需要先修复模拟器状态或更换有效 destination。不要为了让 CI/测试通过而修改业务逻辑。

### 6.14 不要未经要求 push、PR 或清理分支

当前只要求本地继续开发。任何 push、PR、删除 worktree/分支、重写历史都需要用户明确指示。

## 7. 关键文件入口

- `HappaEcho/App/HappaEchoApp.swift`：应用入口与 SwiftData container
- `HappaEcho/App/ContentView.swift`：当前应用壳
- `HappaEcho/Models/`：SwiftData 模型
- `HappaEcho/Features/Chat/ChatSessionController.swift`：聊天生命周期
- `HappaEcho/Features/Attachments/`：原图导入和存储
- `HappaEcho/Features/Notion/NotionClient.swift`：Notion HTTP client
- `HappaEcho/Features/Notion/NotionSyncCoordinator.swift`：Notion actor、队列、恢复和检查点
- `HappaEcho/Features/Settings/SettingsRepository.swift`：Keychain-backed settings access
- `HappaEchoTests/Notion/NotionSyncCoordinatorTests.swift`：Task 9 核心 fixture/fake
- `HappaEchoTests/Notion/NotionClientTests.swift`：Notion HTTP payload tests
- `docs/manual-integration-checklist.md`：已有手动验收项目

## 8. 一句话恢复点

**直接在 `/Users/oyamahappa/Documents/GitHub/HappaEcho` 的 `main` 分支继续；Task 1–9 已合入，下一步从 Task 10 设置 UI 开始，先写失败测试，完成后继续 Task 11–14，并始终保留 iOS 17、Keychain-only secrets、原图字节、SwiftData authoritative 和 Notion 幂等备份约束。**
